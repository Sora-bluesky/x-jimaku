# x-jimaku

[English](README.md) | [日本語](README.ja.md)

X (x.com) の動画にリアルタイム日本語字幕を重ねる Chrome 拡張（Manifest V3）。

<!-- screenshot -->

## 特徴

- X（Twitter）の動画にリアルタイムで日本語字幕をオーバーレイ表示
- 音声認識・翻訳はすべて端末内処理。音声もテキストも外部へ送信しない
- タブ共有ではなく動画要素を直接タップする方式のため、タブ共有インジケーターが出ず、タブの音もそのまま聞こえる
- ツールバーアイコンまたはキーボードショートカット（Ctrl+Shift+9）で動画ごとにON/OFF切り替え
- Whisper モデルを4サイズ（tiny / base / small / turbo）から選択し、速度と精度のバランスを調整可能
- WebGPU 非対応環境では自動的に WASM にフォールバック

## 仕組み

content script が対象の `<video>` 要素の音声を `captureStream()` で直接タップし、16kHz PCM にリサンプルして offscreen document 内の Whisper（transformers.js 経由）へ送る。WebGPU が使える環境ではそちらで、使えない環境では WASM に自動フォールバックしてローカルで英語音声認識を行う。認識したテキストは端末内で英語から日本語へ翻訳する。既定は Chrome 内蔵の Gemini Nano（Prompt API）で、直前の行と画面上の固有名詞を文脈として渡す。モデルが使えない環境では Chrome 内蔵の Translator API に自動で切り替わる。結果は動画下部の高さ固定の字幕バーに表示される。既定では日本語のみで、オプションページの設定で英語原文の小さい行を併記できる。翻訳は認識が確定した節から順に行われる。

```
video要素 --captureStream()--> 16kHz PCM
   --> offscreen document: Whisper (transformers.js, WebGPU/WASM) --> 英語テキスト
   --> Chrome 内蔵 AI（Gemini Nano prompt・不可時は Translator API） --> 日本語テキスト
   --> 動画下部の字幕オーバーレイ
```

## インストール

Chrome ウェブストア未公開のため、GitHub Release から導入する。

1. [Releases](../../releases) ページから `dist` の zip をダウンロードして展開する。
2. `chrome://extensions` を開く。
3. 右上の「デベロッパーモード」をONにする。
4. 「パッケージ化されていない拡張機能を読み込む」で展開した `dist` フォルダを選択する。

## 使い方

1. x.com で動画を再生する。
2. ツールバーの拡張アイコンをクリックする（または Ctrl+Shift+9）。未読み込みの対応動画にはタグが表示される。
3. モデル読み込み中は対象動画に「字幕 準備中…%」と表示され、字幕開始後は「字幕ON」チップに変わる。
4. もう一度クリックすると停止する。対応していない動画には「対象外」バッジが表示される。

## モデル

オプションページで `tiny` / `base`（既定・約150MB） / `small` / `turbo` から選択する。選んだモデルは字幕を初めて開始したタイミングで Hugging Face からダウンロードされ、ダウンロード中は進捗チップが表示される。

## プライバシー

音声・テキストとも外部へ送信されない。Whisper による音声認識は offscreen document 内でローカル実行し（モデルは初回のみ Hugging Face から取得しキャッシュ）、翻訳には Chrome 内蔵の端末内モデル（既定 Gemini Nano・不可時は Translator モデル）を使う。サーバー側コンポーネントやテレメトリは存在しない。 なお、画面に出した字幕はこの端末に記録される（下の「表示ログ」）。

## 表示ログ

画面に出した字幕を、この端末の Chrome ストレージだけに残す。**既定でオン。**

1 ページ出るたびに、表示した日本語の 1 行目と 2 行目（折り返した形のまま）、その元になった英語の文、どのまとまりの何ページ目か、出た時刻と消えた時刻を記録する。折り返し位置と表示時間だけの数字ではない。視聴した動画の字幕が本文ごと残る。

上限は直近 400 ページ。1 ページ 3 秒として、およそ 20 分ぶん。古いものから捨てる。外部には送らない。

不具合報告のために入れた。これまでは「こう見えた」という記憶から直していて、こちらは似た場面を探して当てにいくしかなかった。

入切・コピー・ダウンロード・消去はオプションページの「表示ログ」にまとまっている。チェックを外すと以降は記録しないが、外す前の分は残る。消すときは「表示ログを消す」を押す。

Chrome のプロファイルを誰かと共有しているなら、その人もオプションページから中身を読める。

## 動作要件

- Chrome 138 以降（Translator API の要件）
- WebGPU 推奨（NVIDIA GPU 等）。実用的な速度に必要。WebGPU がない場合は WASM にフォールバックするが、体感できる程度遅くなる
- 翻訳の事前準備は通常不要。端末内の Gemini Nano が使えない環境では Translator API に切り替わり、Chrome が言語パックを自動取得する。翻訳が表示されない場合はオプションページの診断を確認する

## Tips

発話が速い動画では、X プレイヤーの再生速度を 0.75x 等に落とすとパイプラインの処理が追いつきやすくなり、字幕の遅延が目立ちにくくなる。

## トラブルシューティング

- x.com のタブを背面にすると字幕が止まる・「音声がありません」と表示される: Chrome は背面タブの消音動画を省電力のため停止するので、拡張に渡る音声がゼロになる。字幕の利用中は x.com のタブを前面に保つ

## 制限事項

- 音声認識の対象は英語のみ。音声の言語設定を「自動」にしても、英語以外の音声は原文（非日本語）のまま表示される
- 字幕の表示にはライブ再生から数秒の遅延がある
- 個別に開いた動画を1本だけ字幕表示する使い方を想定している

## 利用範囲について

x-jimaku は個人のローカル利用を前提に作られている。自分のブラウザが既に表示しているものをその場で後処理するだけで、音声・文字起こし・訳文が端末の外に出ることはない。音声そのものは残らないが、画面に出した字幕とその元になった英文は端末内に記録される（上の「表示ログ」）。拡張自身が x.com へリクエストを発行することもない（X API 不使用・クロールなし・ダウンロード機能なし）。公開にあたり、この設計を X の利用規約（2026 年 4 月 10 日発効版）および日本の著作権法の私的使用の規定と照らして確認している。

利用・フォークの際もこの前提を保ってほしい。文字起こしの保存・共有、動画のダウンロード、自動巡回のような機能はこのプロジェクトの範囲外とする — 上記の規約・法の線を越えるため。

## ライセンス

MIT
