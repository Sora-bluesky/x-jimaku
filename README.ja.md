# x-jimaku

[English](README.md) | [日本語](README.ja.md)

X (x.com) の動画にリアルタイム日本語字幕を重ねる Chrome 拡張（Manifest V3）。

<!-- screenshot -->

## 特徴

- X（Twitter）の動画にリアルタイムで日本語字幕をオーバーレイ表示
- 音声認識・翻訳はすべて端末内処理。音声もテキストも外部へ送信しない
- タブ共有ではなく動画要素を直接タップする方式のため、タブ共有インジケーターが出ず、タブの音もそのまま聞こえる
- ツールバーアイコンまたはキーボードショートカット（Ctrl+Shift+9）で動画ごとにON/OFF切り替え
- Whisper モデルを4サイズ（tiny / base / small / turbo）から選択し、速度と精度のバランスを調整可能
- WebGPU 非対応環境では自動的に WASM にフォールバック

## 仕組み

content script が対象の `<video>` 要素の音声を `captureStream()` で直接タップし、16kHz PCM にリサンプルして offscreen document 内の Whisper（transformers.js 経由）へ送る。WebGPU が使える環境ではそちらで、使えない環境では WASM に自動フォールバックしてローカルで英語音声認識を行う。認識したテキストは Chrome 内蔵の Translator API で英語から日本語へ翻訳する（これも端末内処理）。結果は動画下部にオーバーレイ表示され、日本語字幕の下に小さく英語原文が添えられる。確定していない暫定行は、音声が増えるごとに約2秒おきに再翻訳される。

```
video要素 --captureStream()--> 16kHz PCM
   --> offscreen document: Whisper (transformers.js, WebGPU/WASM) --> 英語テキスト
   --> Chrome Translator API（en → ja・端末内処理） --> 日本語テキスト
   --> 動画下部の字幕オーバーレイ
```

## インストール

Chrome ウェブストア未公開のため、GitHub Release から導入する。

1. [Releases](../../releases) ページから `dist` の zip をダウンロードして展開する。
2. `chrome://extensions` を開く。
3. 右上の「デベロッパーモード」をONにする。
4. 「パッケージ化されていない拡張機能を読み込む」で展開した `dist` フォルダを選択する。

## 使い方

1. x.com で動画を再生する。
2. ツールバーの拡張アイコンをクリックする（または Ctrl+Shift+9）。未読み込みの対応動画にはタグが表示される。
3. モデル読み込み中は対象動画に「字幕 準備中…%」と表示され、字幕開始後は「字幕ON」チップに変わる。
4. もう一度クリックすると停止する。対応していない動画には「対象外」バッジが表示される。

## モデル

オプションページで `tiny` / `base`（既定・約150MB） / `small` / `turbo` から選択する。選んだモデルは字幕を初めて開始したタイミングで Hugging Face からダウンロードされ、ダウンロード中は進捗チップが表示される。

## プライバシー

音声・テキストとも外部へ送信されない。Whisper による音声認識は offscreen document 内でローカル実行し（モデルは初回のみ Hugging Face から取得しキャッシュ）、翻訳には Chrome 内蔵の端末内翻訳モデルを使う。サーバー側コンポーネントやテレメトリは存在しない。

## 動作要件

- Chrome 138 以降（Translator API の要件）
- WebGPU 推奨（NVIDIA GPU 等）。実用的な速度に必要。WebGPU がない場合は WASM にフォールバックするが、体感できる程度遅くなる
- 翻訳の事前準備は通常不要（Chrome が言語パックを自動取得する）。翻訳が表示されない場合はオプションページの診断を確認する

## Tips

発話が速い動画では、X プレイヤーの再生速度を 0.75x 等に落とすとパイプラインの処理が追いつきやすくなり、字幕の遅延が目立ちにくくなる。

## 制限事項

- 音声認識の対象は英語のみ。音声の言語設定を「自動」にしても、英語以外の音声は原文（非日本語）のまま表示される
- 字幕の表示にはライブ再生から数秒の遅延がある
- 個別に開いた動画を1本だけ字幕表示する使い方を想定している

## ライセンス

MIT
